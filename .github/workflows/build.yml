jobs:
  my-job:
    runs-on: windows-latest
    env:
      CLIENT_ID: ${{ secrets.CLIENT_ID }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      # Add more steps as needed
